"""Limen: threshold-gated safety architecture for confidence-coupled embedded control.

This package provides:
- Confidence-Coupled Safety Envelope (``limen.safety``)
- Eventized multi-rate scheduler (``limen.scheduler``)
- EMG/IMU signal pipeline: generation, filtering, feature extraction, inference
- Profile compiler (``limen.profile``)
- Capability negotiation protocol (``limen.protocol``)
- Replay benchmark with telemetry and deterministic fault injection
"""

__version__ = "0.1.0"
